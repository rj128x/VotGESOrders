<UserControl xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"  x:Class="VotGESOrders.Views.DateTimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid x:Name="LayoutRoot"
        Background="White">
		<sdk:DatePicker Height="23"
                    HorizontalAlignment="Left"
                    Name="DatePicker"
                    VerticalAlignment="Top"
                    Width="120"  SelectedDate="{Binding SelDate,Mode=TwoWay}"
							 IsEnabled="{Binding Enabled}"/>
		<toolkit:TimePicker Height="22"
                        HorizontalAlignment="Left"
                        Margin="126,0,0,0"
                        Name="TimePicker"
                        VerticalAlignment="Top" Value="{Binding SelTime, Mode=TwoWay}"
								  IsEnabled="{Binding Enabled}"/>
	</Grid>
</UserControl>
